log_level: info
output_format: json
data_dir: /data

discovery:
  sources:
    agent: 
      enabled: true
      collect_software: true
      collect_interfaces: true

streaming:
  interval: 60s
  otlp:
    endpoint: http://localhost:4318
    protocol: http

metrics:
  enabled: false
  # listen: :9091